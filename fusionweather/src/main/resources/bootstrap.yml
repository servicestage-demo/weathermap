server:
  port: 10392

spring:
  application:
    name: fusionweather
  cloud:
    servicecomb:
      discovery:
        version: 0.0.2
        address: http://127.0.0.1:30100
        watch: false
        servieName: ${spring.appliction.name}
        appName: weathermap
      config:
        serverAddr: http://127.0.0.1:30110
        serverType: kie