server:
  port: 13093

spring:
  application:
    name: weather
  cloud:
    servicecomb:
      discovery:
        address:  http://127.0.0.1:30100
        serviceName: ${spring.application.name}
        appName: weathermap
        version: 0.0.3
        watch: false
      config:
        serverAddr: http://127.0.0.1:30110
        serverType: kie