server:
  port: 13093

spring:
  application:
    name: weather
  cloud:
    servicecomb:
      discovery:
        enabled: true
        address: https://cse.cn-north-1.myhuaweicloud.com
        serviceName: ${spring.application.name}
        appName: weathermap
        version: 0.0.3
        watch: false