server:
  port: 13091

spring:
  application:
    name: forecast
  cloud:
    servicecomb:
      discovery:
        watch: false
        address: http://127.0.0.1:30100
        version: 0.0.2
        serviceName: ${spring.application.name}
        appName: weathermap
      config:
        serverAddr: http://127.0.0.1:30110
        serverType: kie

